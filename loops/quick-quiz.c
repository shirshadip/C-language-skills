#include <stdio.h>

int main(void) {
    int i =0;
    while (i<20+1)
    {
        /* code */
        if (i>=10){

            printf("the value of i is --> %d\n",i);
        }
        i++;
    }
    
    return 0;  // End of program
}
