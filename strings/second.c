#include <stdio.h>
#include <string.h>
int main() {
    // Your code goes here
    // char st[30];
    // gets(st);
    // puts(st);
    // printf("%s",st);
    char st[]= "sh";
    printf("%d",strlen(st));

    return 0; // Indicates successful program execution
}
